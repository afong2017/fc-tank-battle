# AI 自动验收与升级

## 已实现的流程

正式策略快照 → 小幅候选参数变化 → 第 1、4、6 关筛选 → 全部 35 关对照及生成地图验证 → 验收 → 下一局启用 → 监测和回退。

- 候选只调整有限范围内的参数，不修改游戏代码、物理速度、生命值或安全规则。
- 正常游戏继续记录经验，但不随机探索，也不直接采用局部奖励或旧调参器产生的变化。
- 一局内使用固定快照。发布、回退均在后续新一局生效；热更新交接保留本局快照。
- 候选参数以 0.1、0.25 或 0.5 的幅度变化。当前实现是受控参数搜索，不是自动编程，也不是神经网络训练。
- 失败候选不增加正式代数。没有足够证据就不发布，允许长期停留在同一代。

## 启动和查看

在游戏目录终端执行：

```powershell
pnpm ai:evolve -- --jobs 20
pnpm ai:release-status
```

第一条命令最多串行执行 20 局隔离模拟，结束即停止。重复执行会接着上次进度继续。省略 `--jobs` 时默认只执行 2 局；这不是后台常驻进程，不会另开游戏页面。游戏正常运行不依赖训练进程。

完整候选最多需要 6 局筛选和 760 局验证，可能耗时很长。不能把 1 秒冒烟测试当作战斗成绩：

```powershell
pnpm ai:evolve -- --smoke --jobs 2
```

手工回退：

```powershell
pnpm ai:release-status -- --rollback
```

## 验收约束

新旧策略必须使用相同代码、物理规则、地图和随机种子。每关至少 10 组配对，另有至少 3 张生成地图、30 组配对。验证不回写学习数据。重复地图、未完成对局、缺失指标、版本不符或安全违规均被拒绝。

不允许任何已赢对局变输，也不允许配对的阵亡、绕圈、停滞次数增加；同样获胜时不能更慢。至少 7 组必须有实际改善。这个门槛是保守验收规则，不是“所有未知局面必然更强”的数学证明。

模拟器记录误拆保护砖、误击老巢和伤害队友。正式对局通过现有事件检测误击老巢、队友误伤死亡；这些事件或明显的分关胜率退步可触发回退。正式游戏尚无完整的保护砖受伤事件，因此这项安全检查目前在隔离模拟中完成。

## 记录与恢复

- `test-results/evolution/`：只用于测试的快照、逐局结果、进度、验收报告。
- `policy-releases/registry.json`：正式策略、上一个可回退策略、发布历史及监测记录。
- 不删除、不清零游戏数据库，也不把隔离模拟计入正常训练局数。
- 中途停止后可以续跑；代码变化会拒绝旧证据，不能强行修改指纹继续使用。
- 异常退出若遗留锁文件，先核实对应进程已经退出，再处理锁文件。
- 如需开始新的独立实验，可设置 `AI_EVOLUTION_DIRECTORY` 为新的测试结果目录。

## 边界

发布流程已连接，但开发测试通过不表示某个候选策略已通过完整战斗验收。当前代数和是否启用新策略，以 `ai:release-status` 为准。真实帧率及机器负载仍可能影响浏览器表现；离线模拟采用固定物理步长和确定性搜索节点上限，不能替代长期实际对局观察。
